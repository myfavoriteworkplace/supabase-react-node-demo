# React + Node + Supabase Demo

## Local Setup
1. Create a Supabase project (Free Plan).
2. Copy your Project URL, Anon Key, and Service Role Key.
3. Backend:
   ```bash
   cd backend
   npm install
   npm run dev   # uses .env.local
Frontend:

cd frontend
npm install
npm start     # uses .env.local

http://localhost:3000.

